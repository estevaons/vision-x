version: '3.8'

services:
  vision-x-server:
    build: .
    ports:
      - "3001:3001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./:/usr/src/app
    command: node server.js
